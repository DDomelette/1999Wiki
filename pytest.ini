[pytest]
testpaths = tests
markers =
    registry_integration: uses disposable local OCI registry containers
norecursedirs =
    .git
    .local
    backups
    data
    dist
    eval
    node_modules
    playwright-report
    test-results
    vectorstore
    infra/milvus/volumes
