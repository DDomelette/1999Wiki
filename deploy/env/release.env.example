# Validation-only release metadata. Copy per slot outside Git and replace the
# sha-replace-before-production tags with both images from one immutable commit.
BACKEND_IMAGE=ghcr.io/ddomelette/1999wiki-backend:sha-replace-before-production
FRONTEND_IMAGE=ghcr.io/ddomelette/1999wiki-frontend:sha-replace-before-production
BACKEND_PORT=18100
FRONTEND_PORT=18180
