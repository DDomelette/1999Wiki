# Validation-only release metadata. Copy values from release-manifest.json;
# replace every placeholder before production.
RELEASE_COMMIT=replace-with-full-40-character-commit
BACKEND_IMAGE=ghcr.io/ddomelette/1999wiki-backend:sha-replace@sha256:replace-with-backend-registry-digest
FRONTEND_IMAGE=ghcr.io/ddomelette/1999wiki-frontend:sha-replace@sha256:replace-with-frontend-registry-digest
BACKEND_PORT=18100
FRONTEND_PORT=18180
