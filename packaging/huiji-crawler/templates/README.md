# HuijiWiki crawler for Windows

Run `install.cmd` once with Windows x64 CPython 3.12 available. Then use `huiji-crawler.cmd doctor` and `huiji-crawler.cmd crawl` from this directory.

Credentials, browser profiles and crawl output are created only below this tool directory and are not included in the ZIP.
